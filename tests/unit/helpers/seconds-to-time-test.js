import {
  secondsToTime
} from 'spotbot-client/helpers/seconds-to-time';

import {module, test} from 'qunit';

module('SecondsToTimeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = secondsToTime(42);
  assert.ok(result);
});
